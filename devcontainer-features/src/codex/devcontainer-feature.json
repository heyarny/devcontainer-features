{
  "id": "codex",
  "version": "3.0.0",
  "name": "OpenAI Codex CLI",
  "description": "Installs the standalone OpenAI Codex CLI globally without requiring Node.js or npm, and optionally links Codex state folders into the workspace.",
  "documentationURL": "https://github.com/openai/codex",
  "entrypoint": "/usr/local/share/codex/entrypoint.sh",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Codex CLI release version to install. Use 'latest' to resolve the newest release at build time. The minimum supported release is 0.146.1."
    },
    "installDir": {
      "type": "string",
      "default": "/usr/local/bin",
      "description": "Directory where the global codex command symlink is installed."
    },
    "standaloneHome": {
      "type": "string",
      "default": "/usr/local/share/codex",
      "description": "Directory where standalone Codex release payloads are stored."
    },
    "linkFolders": {
      "type": "string",
      "default": "",
      "description": "Optional folder link mappings. Target paths must resolve to absolute container paths. Omit this option when no folder links are needed."
    },
    "configSyncSource": {
      "type": "string",
      "default": "",
      "description": "Optional absolute container path to a mounted config.toml file to sync bidirectionally with $CODEX_HOME/config.toml. Startup waits up to 15 seconds for the initial sync check, then continues while synchronization waits or retries as needed. Omit this option to disable config syncing."
    }
  }
}
